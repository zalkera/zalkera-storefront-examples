# CUSTOMIZE.md — 무엇을 어디서 고치나

이 zip 은 **당신의 사이트 소스**입니다. 고쳐도 되고, 다른 AI 에게 맡겨도 되고, 개발자에게 줘도 됩니다.
같은 결과에 이르는 길이 여러 개라, **어디서 고치는 게 가장 싸고 빠른지**와 **고칠 때 함께 유지해야
콘솔 기능이 사는 배선**이 무엇인지 — 이 문서가 그 지도입니다.

> 개발자·AI 에이전트용 기술 규약은 [`AGENTS.md`](AGENTS.md), 셋업·구조는 [`README.md`](README.md) 에 있습니다.
> 이 문서는 **"내가 무엇을 어디서 고쳐야 하나"** 만 답합니다.

---

## 0. 먼저 — 무엇이 어디에 사는지

이 zip 은 **사이트의 얼굴 전부**를 담고 있습니다. 페이지·문구·섹션 순서·내비는 `content/` 안의 파일이고,
그것이 **정본**입니다. 콘솔이 아니라 여기서 고칩니다.

| 사는 곳 | 무엇 | 고치는 법 |
|---|---|---|
| **소스**(이 zip) | 페이지·문구·섹션 순서·내비·디자인 | 파일을 고쳐 **재업로드**(§4) |
| **콘솔**(데이터베이스) | 상품·가격·재고·주문·회원·게시글·사이트 설정 | 콘솔에서 바로 — 재업로드 불필요 |

팩은 고객 DB 에 **아무것도 만들지 않습니다.** 색은 소스의 `@theme`, 얼굴은 `content/`, 업무
데이터(상품·주문·회원)는 콘솔이 주인입니다.

⚠ **상품 목록·시술 메뉴 같은 진열은 소스에 있습니다**(`src/components/ProductRail.tsx`). 콘텐츠 파일이
아니라 tsx 입니다 — 상품의 정본 값은 콘솔 뒤 데이터베이스에 살고 화면은 그것을 비추기만 하므로, "무엇을
어디에 어떻게 뿌릴지"는 소스가 정합니다. 그래서 *"여기에 인기 상품 네 개만 가로로 보여줘"* 같은 지시가
그대로 먹습니다. 상품 자체를 늘리고 줄이는 것은 콘솔에서 하고, 재업로드가 필요 없습니다.

> **zip 을 먼저 올려도 빈 사이트가 되지 않습니다.** 얼굴이 `content/` 에 있으므로 올리면 페이지·문구가
> 그대로 따라옵니다 — 콘솔 작업과의 순서를 걱정할 필요가 없습니다.

## 1. 콘솔에서 바로 되는 것 — 무료·즉시

가장 많이 바꾸고 싶어 하는 것들이 여기 있습니다. **소스를 열지 않아도, 재업로드하지 않아도 바뀝니다.**

> **페이지 문구·섹션 순서·메뉴는 여기 없습니다.** 그것은 소스(`content/`)가 정본이라 §2 에서 고칩니다.

| 바꾸고 싶은 것 | 어디서 | 비용 |
|---|---|---|
| 브랜드 색(강조색·보조색·배경색·글자색) | 소스 — `src/app/globals.css` | 무료·다시 빌드 |
| 글꼴 / 모서리 둥글기 / 여백 밀도 | 소스 — `src/app/globals.css` | 무료·다시 빌드 |
| 상품·가격·재고·게시글 | 콘솔 | 무료 |
| 상호·대표자·사업자등록번호·주소·전화·이메일 | 콘솔 — 사이트 설정 | 무료 |
| 구매 정책(반품·교환·배송·A/S) 문구 | 콘솔 — 사이트 설정 | 무료 |

색·타이포의 **정본은 `src/app/globals.css` 의 `@theme` 하나**입니다. 콘솔에는 색 설정이 없습니다 —
값을 두 곳에 두면 어느 쪽이 이기는지가 팩마다 달라지고, 콘솔에서 바꿨는데 화면이 안 움직이는 일이
생깁니다.

거기서 고치는 것들:

- **색** — `--color-primary`(강조) · `--color-secondary`(보조) · `--color-background`(배경) ·
  `--color-foreground`(본문 글자). 그 밖의 중립 스케일(`--color-muted`·`--color-border` 등)도 같은
  자리에 있습니다.
- 🔴 **`--color-primary-foreground` 를 반드시 함께 고르십시오.** `primary` 위에 얹히는 글자색이고
  기본값이 **흰색**입니다. 밝은 브랜드색(파스텔·노랑 계열)을 넣으면 흰 글자가 안 읽힙니다 —
  그 경우 이 값을 어두운 색으로 바꾸십시오. 쓰는 자리: 버튼·CTA(`text-primary-foreground`).
- **글꼴** — `--font-sans`. ⚠ 기본 팩은 폰트 파일을 동봉하지 않아 **방문자 기기에 설치된 글꼴만**
  적용됩니다(없으면 시스템 스택으로 폴백). 확실히 바꾸려면 폰트 파일을 `public/fonts/` 에 넣고
  `@font-face` 를 함께 선언하십시오.
- **모서리** — `--radius-knob`(무단위 배수. `0`=각짐 · `1`=기본 · `2`=곱절)
- **여백 밀도** — `--spacing`(기본 `0.25rem`. 줄이면 전 여백이 함께 좁아집니다)

> 고치면 **다시 빌드해야** 화면에 나갑니다. 색만 바꾸는 일은 드물고 대개 디자인 손질과 함께
> 오므로, 그 비용은 한 번에 치릅니다.

---

## 2. 소스에서 고치는 것 — 어디를 열어야 하나

| 고치고 싶은 것 | 파일 | 난이도 |
|---|---|---|
| 색·글꼴 | `src/app/globals.css` 의 `@theme` 블록 **한 곳** | 쉬움 |
| 홈의 골격(시드 페이지가 없을 때 그려지는 화면) | `src/app/page.tsx` | 보통 |
| 상단 메뉴 영역 / 하단 영역 | `src/components/SiteHeader.tsx` · `SiteFooter.tsx` | 보통 |
| 섹션(히어로·특징·후기·FAQ…)의 생김새 | `src/components/sections/*.tsx` | 보통 |
| 버튼·카드·아이콘 모양 | `src/components/ui/*.tsx` | 보통 |
| 상품·장바구니·결제 화면 | `src/app/products` · `cart` · `checkout` | 어려움 |
| 새 페이지 추가 | `content/pages/<이름>.json` **+ `content/index.ts` 두 줄** **또는** `src/app/<경로>/page.tsx` | 쉬움 / 보통 |

### 페이지를 늘리는 길은 둘이고, 둘 다 정상입니다

**⑴ 콘텐츠 파일** — `content/pages/회사연혁.json` 을 넣고 `content/index.ts` 에 **두 줄**을 더하면
`/회사연혁` 이 생깁니다. 섹션 어휘로 적으니 우리 컴포넌트가 그려 주고, 나중에 콘솔 편집을 붙이기도 쉽습니다.

```ts
// content/index.ts
import history from "./pages/회사연혁.json";   // ← 한 줄

export const pages: Record<string, unknown> = {
    회사연혁: history,                        // ← 한 줄
};
```

**json 만 넣으면 페이지는 안 생깁니다.** 정적 import 가 없으면 빌드 산출물에 콘텐츠가 안 실리고,
`npm run validate` 가 `[N3]` 으로 막습니다(위 표 참조). 파일 이름은 한글이어도 됩니다.

**⑵ 손으로 짠 라우트** — `src/app/회사연혁/page.tsx` 를 직접 만듭니다. 섹션 어휘를 안 써도 되고,
마크업을 마음대로 짜면 됩니다. **막는 규칙이 없습니다** — 라우트를 몇 개 만들든, 어떤 모양이든 자유입니다.

둘 중 무엇을 고르든 우리는 묻지 않습니다. 섞어 써도 됩니다.

### 검색·AI 노출도 당신이 정합니다

`robots.txt`·`sitemap.xml`·JSON-LD 를 안 넣어도 **사이트는 정상 작동합니다.** 우리가 검사하는 것은
당신이 **스스로 주장했을 때**뿐입니다 — 팩에 "이건 쇼핑몰이다" 같은 분류를 걸면 그 분류가 약속한 표면을
내는지 봅니다. 아무것도 주장하지 않으면 잴 것이 없습니다.

즉 **SEO 를 포기하고 원하는 대로 짜는 것도 선택지입니다.** 대가는 검색·AI 에 덜 노출되는 것이고,
그건 당신 몫입니다. 나중에 마음이 바뀌면 파일 몇 개 추가로 얹을 수 있습니다(마크업·CSS 를 안 건드립니다).

### 우리가 서빙할 때 보는 셋 — 둘은 막고, 하나는 경고만 냅니다

우리 인프라에서, 우리가 넣어 준 시크릿 키를 들고 도는 사이트라면 아래 셋을 봅니다. **검사기가 rc 1 로 막는 것은
아래 둘(시크릿 노출 · 서버 렌더 강제)이고, 교차사이트 위조 가드는 어느 모드에서도 경고입니다** — 사람이 봐야 합니다.
재현: 가드 없는 `POST` 라우트를 하나 만들고 `node scripts/validate-storefront.mjs ./src --gate; echo rc=$?`
→ `⚠️  [X1] …` 한 줄과 `✅ 통과 … (경고 1)` · `rc=0`

| 무엇 | 왜 |
|---|---|
| 당신이 만든 `app/api/**` 변이 라우트에 **교차사이트 위조 가드가 없음** | 공격자 페이지가 **당신 사이트 방문자**의 브라우저로 요청을 보냅니다. 다치는 건 방문자입니다. ⚠ **이 축은 검사기가 막지 않고 경고만 냅니다** — `npm run validate` 가 통과해도 안전을 뜻하지 않으니 `⚠️ [X1]` 이 보이면 고치십시오 |
| **시크릿이 브라우저 번들에 실림** | 그 키는 우리가 발급했고, 새면 그 테넌트 전체가 열립니다 |
| SEO 라우트가 **요청마다 서버 렌더를 강제** | 그 원가를 우리가 냅니다 |

**자체 호스팅이면 이 셋도 경고입니다.** 우리 인프라도 우리 키도 아니니까요 — `npm run validate` 를
직접 돌리면 경고로만 나옵니다. 기준은 "무엇을 선언했는가"가 아니라 **누가 서빙하는가** 입니다.

> 참고로 **백엔드는 이 가드에 의존하지 않습니다.** 백엔드는 요청의 출처(`Origin`)를 보지 않고 시크릿
> 키로 테넌트를 정합니다 — 유효한 키가 있으면 **어느 서버에서든** 부를 수 있습니다. 위 가드는 백엔드
> 보호가 아니라 **당신 사이트 방문자 보호**입니다.

### 이미지는 두 군데에 나뉘어 있습니다

**상품 이미지는 콘솔에 있습니다.** 상품·후기 같은 업무 데이터의 그림은 콘솔에 올린 미디어를
`/media/{id}` 주소로 불러오므로, 교체도 콘솔에서 합니다. 소스를 고쳐도 이 그림은 안 바뀝니다.

**화면 장식 이미지는 소스에 있을 수도, 없을 수도 있습니다.** 히어로 배경이나 로고 같은 것을 동봉한
템플릿이면 `public/images/` 에 들어 있고, 바꾸려면 파일을 갈아 끼우고 재업로드하면 됩니다. 반대로
**장식 이미지를 하나도 안 쓰는 템플릿이면 `public/` 자체가 없습니다** — 골격만 받아 직접 꾸미는
쪽이라 그렇습니다. 어느 쪽인지는 받은 zip 에서 바로 확인하십시오:

```bash
ls public/images 2>/dev/null || echo "이 템플릿에는 동봉된 장식 이미지가 없습니다"
```

동봉된 이미지가 있으면 그 출처·라이선스가 `.zalkera/ASSETS-LICENSE.md` 에 적혀 있습니다. 없는
템플릿이면 그 파일도 "이미지가 없습니다"라고 말합니다 — **둘은 항상 같은 답을 합니다.**

`.zalkera/pack.json` 은 이 zip 이 **어느 템플릿의 몇 번 판인지** 적어 둔 쪽지입니다(잘커라 카탈로그가
쓰는 표식). **재업로드 전에 지우십시오** — 서버가 팩 신원을 그 파일에서 읽으므로, 남기면 당신 사이트가
그 카탈로그 팩의 신원으로 적재됩니다. 받은 판본을 기억해 두고 싶으면 파일 밖에 적어 두십시오.

---

### 헤더·푸터는 **당신 것입니다**

`src/components/SiteHeader.tsx`·`SiteFooter.tsx` 는 마음대로 고쳐도 됩니다. 드로어·스티키·메가메뉴
무엇이든 여기서 만드십시오 — 반응형까지 가면 UI/UX 가 매우 가변적이라 **하드코딩이 정답인 자리**이고,
어떤 설정값도 이 화면을 강제하지 않습니다.

특히 **장바구니·로그인 링크는 그 기능을 실제로 쓸 때만 두십시오.** 이 골격은 쇼핑몰·예약까지 전부
배선된 상태로 오는데, 소개 사이트에 장바구니가 있으면 눌러도 늘 비어 있어 **사이트가 자기 정체를
잘못 말하게 됩니다.** 지우는 것이 맞습니다 — 무엇을 함께 지우면 되는지는 `AGENTS.md` 의
"능력 ↔ 구현 좌표" 표에 있습니다.

### 올리기 전에 내 컴퓨터에서 보려면

`npm run dev` 는 **잘커라 백엔드에 붙습니다** — 상품·설정 같은 데이터를 거기서 읽기 때문입니다.
`.env.local` 에 `ZALKERA_API_BASE`·`ZALKERA_TENANT` 를 자기 값으로 넣으십시오(`.env.example` 참고).

사이트가 이미 열려 있다면 `NEXT_PUBLIC_ZALKERA_PREVIEW=1` 을 함께 주십시오. 실제 데이터로 화면을
보면서도 장바구니·주문·결제·문의 같은 **쓰기가 403 으로 막힙니다.** 로그인·로그아웃은 그 화면들을
봐야 하므로 막지 않습니다 — 자기 세션만 건드립니다.

차단은 `src/middleware.ts` **한 곳**에서 일어납니다 — 요청의 메서드와 경로만 보므로 선언 형태·
파일명과 무관하게 **새로 만든 라우트도 아무것도 안 하고 덮입니다.**

막지 않는 자리는 `src/lib/previewGuard.ts` 의 `PREVIEW_WRITE_ALLOW` 목록에 있고, 각 라우트 파일
머리의 `// zalkera-allow-preview-write: <이유>` 마커에 사유가 적혀 있습니다.

> ⚠ **예외가 하나 있습니다.** matcher 가 빼는 접두(`_next/static`·`_next/image`·`images/`·
> `favicon.ico`) 밑에 쓰기 라우트를 만들면(예: `src/app/images/` 아래) **관문 밖**입니다.
> 그 형상은 빌드 뒤에 이것으로 확인하십시오:
>
> ```bash
> npm run build && node scripts/lib/gate-probe.mjs
> ```

> ⚠ `ZALKERA_OFFLINE_BUILD` 는 **이 용도가 아닙니다.** 백엔드 없이 빌드를 돌리는 CI 를 위한
> 플래그이고, 화면 확인에 쓰는 물건이 아닙니다. (덧붙이면 이 플래그는 **지금 아무것도 바꾸지 않습니다** —
> 읽는 코드에 호출자가 없습니다. `.env.example` 의 설명을 보십시오.)

---

## 3. 깨뜨리면 안 되는 규약 — 검사기가 강제하는 것

올리기 전에 이 두 줄을 돌리십시오.

**서버가 `validate` 를 돌려 준다고 기대하지 마십시오.** 함께 도는지는 소스 출처에 따라 다르고,
안 도는 경우 규약을 어긴 채로도 빌드만 되면 서빙까지 갑니다. 그러면 사이트는 뜨는데 콘솔 기능(섹션
편집 같은 것)이 조용히 죽습니다. 그래서 `validate` 는 **당신이 직접** 돌려야 하는 검사입니다.

```bash
npm run validate     # 규약 검사 — 위반이 있으면 exit 1
npm run build        # 실제 빌드 — 실패하면 업로드해도 서빙되지 않습니다
```

zip 통째로 검사하고 싶으면(형식·시크릿·규약·빌드를 한 번에):

```bash
node scripts/verify-zip.mjs 내가고친것.zip
```

### `npm run validate` 가 보는 것

이 레포는 `package.json` 에 `"zalkera": {"styling": "tailwind-tokens"}` 를 **선언**하고 있어서,
아래 **스타일 축(S)** 이 경고가 아니라 오류로 막힙니다(선언한 계약을 지키는지 보는 것입니다).

⚠ **나머지 축은 `npm run validate` 에서 경고입니다 — 통과가 안전을 뜻하지 않습니다.**
검사기가 무엇을 격상하는지 스스로 찍습니다:

```bash
npm run validate | grep '규약 모드'
# 스타일 규약 모드: declared (tailwind-tokens 계약 — S2/S4 error 격상)
# 콘텐츠 규약 모드: declared (content=source 계약 — N 규칙 error)
```

**`npm run validate` 가 rc 1 로 막는 것**

| 코드 | 무엇을 막나 | 왜 |
|---|---|---|
| **S1** | tsx·jsx 안의 죽은 옛 CSS 변수(`var(--oneq-*)`) 참조 | 색이 안 나옵니다 |
| **S2** | JSX 인라인 `style={{…}}` | 토큰을 우회해 그 자리만 따로 놉니다 — `@theme` 을 고쳐도 안 따라옵니다 |
| **S3** | `src/app/globals.css` 삭제 또는 layout 의 import 누락 | 스타일이 통째로 안 실립니다 |
| **S4** | `bg-[#e91e63]` 같은 색 하드코딩 | `@theme` 을 고쳐도 그 자리만 안 바뀝니다 |
| **S6** | 남의 디자인시스템 토큰 이름(`bg-card`·`text-muted-foreground` 등) | 우리에겐 정의가 없어 **색이 조용히 빠집니다** |
| **S8** | `globals.css` 의 `@theme` 에 `--color-primary` 가 없음 | 색의 정본이 비어 유틸리티가 기본색으로 떨어집니다 |
| **N1~N5** | `content/pages/*.json` 과 `content/index.ts` 의 배선이 어긋나거나, JSON 이 깨졌거나, 가리킨 이미지가 없음 | 페이지가 **조용히 안 생기거나** 개시 후 깨진 이미지가 나옵니다 |
| **D1·D2** | `AGENTS.md`·`llms.txt` 가 **없는 파일**을 가리킴 | codegen 이 이 문서를 가장 먼저 읽습니다 — 죽은 좌표는 AI 를 헤매게 합니다 |

**경고로만 나오는 것 — 보이면 직접 고쳐야 합니다**

| 코드 | 무엇을 막나 | 왜 위험한가 |
|---|---|---|
| **E1·E2** | `"use client"` 파일에서 `@zalkera/client` 나 `src/lib/zalkera.ts` 를 값으로 import | **서버 주소·토큰이 브라우저 번들에 실립니다.** 그 키는 우리가 발급했고 새면 그 테넌트 전체가 열립니다 |
| **C1** | 공개 페이지(홈·목록·상세·글)가 요청마다 서버 렌더하도록 만드는 것 | 느려지고 서버 비용이 상시로 붙습니다 |
| **C1b** | `layout` 이 import 로 닿는 파일에서 `cookies()`·`headers()` 사용 | **그 아래 전 페이지**가 통째로 느려집니다 |
| **C2** | 섹션 렌더러가 계약의 섹션 종류를 빠뜨리는 것 | 콘솔에서 넣은 섹션이 **조용히 안 그려집니다** |
| **S5** | `globals.css` 외의 `.css` 파일 추가 | 색의 출처가 둘로 갈립니다 |
| **X1~X3** | 변이 라우트에 교차사이트 위조 가드 없음 | 공격자 페이지가 **당신 사이트 방문자**의 브라우저로 요청을 보냅니다 |

**E·C 축**은 우리가 서빙할 때(`--gate`) 오류로 올라갑니다. **X 와 S5 는 어느 모드에서도 경고입니다** —
관문도 그것들은 안 막으니, `⚠️ [X1]` 이 보이면 통과했더라도 사람이 고쳐야 합니다.
재현: `node scripts/validate-storefront.mjs ./src --gate; echo rc=$?` → `⚠️ [X1] …` 이 있어도 `rc=0`

⚠ **S1 은 `.css` 파일 안은 안 봅니다.** `globals.css` 에 `var(--oneq-*)` 를 남겨도 안 잡힙니다.

정당한 예외는 **그 파일에** 주석 마커로 사유를 남기면 면제되거나 경고로 내려갑니다
(`// zalkera-allow-dynamic: <이유>` · `// zalkera-allow-inline-style: <이유>` · `// zalkera-allow-custom-theme-inject: <이유>`).
**마커와 사유는 같은 줄**이어야 합니다 — 사유가 없거나 다음 줄에 있으면 면제되지 않습니다.
마커는 **검사만 면제할 뿐 동작을 보장하지 않습니다** — 실제로 되는지는 사람이 한 번 확인해야 합니다.

### 올린 뒤 한 번 더 — 검색·AI 가 읽는지 확인

`npm run validate` 는 **소스**를 봅니다. 그런데 검색·AI 가 실제로 읽는 것은 **개시된 페이지**라, 그쪽은 소스가 아니라
사이트를 크롤해서 잽니다 — 그 검사기와 잣대가 되는 보장표 둘 다 `npm install` 로 따라오는 `@zalkera/client` 안에
들어옵니다(`scripts/check-aeo-surfaces.mjs` 는 그것을 부르는 짧은 연결 파일입니다. 이 프로젝트 안에서 다 해결되므로
**다른 것을 받아올 필요가 없습니다**).

```bash
npm install                 # 아직 안 했다면
npm run check:aeo -- https://내사이트주소 --category BOOKING
```

상품 상세의 가격(`Product`+`Offer`)·목록(`ItemList`)·회사 정보(`Organization`)·고정 페이지(`WebPage`)가
구조화 데이터로 나가는지 봅니다. **소스를 읽지 않으므로 직접 짠 화면이든 이 템플릿 그대로든 같은 잣대**입니다 —
어떻게 짰는지가 아니라 나온 결과만 봅니다. `--category` 는 사이트 유형(`MARKETING`·`PORTFOLIO`·`EVENT`·
`BOOKING`·`BLOG`·`COMMERCE`)입니다. 화면을 직접 짜서 그 유형에 해당하지 않는다면 `--category` 대신 `--site-wide-only` 를
주십시오 — 검색·AI 가 목록을 받아 가는 최소치(robots·sitemap·주소 형식)만 재고, 유형별 요구는 재지 않습니다.

잣대가 되는 **보장표 파일**(`aeo-surface-guarantees.json`)은 `@zalkera/client` 가 싣고 오므로 보통은 신경 쓸 것이
없습니다. 검사기는 ① `--guarantees <경로>` ② 설치된 `@zalkera/client` 안의 보장표 ③ 잘커라 레포와 나란히 둔
체크아웃 순으로 찾고, **어느 것으로 쟀는지 실행할 때마다 찍습니다**. 셋 다 없으면 검사기는 **아무 판정도 하지 않고
멈춥니다**(없는 잣대로 통과시키는 것보다 안 재는 편이 정직합니다). 잣대만 확인하려면 사이트 없이도 됩니다:

```bash
npm run check:aeo -- --print-guarantees
```

무엇이 규범인지는 `node_modules/@zalkera/client/llms.txt` 의 §5.1 에, 그 규범이 이 소스 어디에 구현돼 있는지는
[`AGENTS.md`](AGENTS.md) 의 좌표표에 있습니다.

### 지우면 조용히 고장 나는 것 (검사기가 못 잡는 것 포함)

- **`package.json` 의 `"zalkera": {"styling": "tailwind-tokens"}` 선언.**
  지우면 스타일 규약 검사(S2·S4·S8)가 경고로 내려앉습니다 — 색 리터럴이 흩어져도 안 막힙니다.
- **`src/app/globals.css` 의 `@theme` 블록** — `bg-primary` 같은 클래스가 여기서 생깁니다(S8).
- **`SectionRenderer` 의 `default:` 가 조용히 `null` 인 것** — 새 섹션 종류가 추가돼도 옛 사이트가 안 깨지는 계약입니다.
- **`LEAD_CTA` 섹션의 `id="lead"`** — 홈 첫 화면의 CTA 버튼이 여기를 가리킵니다. 지우면 간판 버튼이 **아무 데도 안 갑니다**.
- **`src/lib/safeUrl.ts`** 를 거치는 링크 처리 — 콘솔 입력으로 들어오는 주소의 방어선입니다.
- **`.env` 파일을 zip 에 넣지 마세요.** 소스는 공개될 수 있고, 한 번 나간 값은 회수할 수 없습니다.

### 플랫폼이 업로드 뒤에 하는 것

올린 소스는 **격리 샌드박스에서 실제로 빌드**됩니다. 빌드가 실패하면 서빙되지 않습니다(이전 사이트는 그대로 살아 있습니다).
규약 검사(`npm run validate`)가 함께 도는지는 소스 출처에 따라 다릅니다 —
**믿지 말고 올리기 전에 직접 돌리십시오.** 위 두 줄이면 됩니다.

---

## 4. 고쳤으면 **다시 올려야** 반영됩니다

내 컴퓨터에서 고친 것은 서빙 중인 사이트에 자동으로 가지 않습니다. 콘솔에서 zip 을 다시 올려야 합니다.

zip 을 다시 쌀 때:

- **`node_modules` 와 `.next` 는 빼십시오.** 들어 있으면 반려됩니다(용량도 수백 MB 가 됩니다).
- `package.json` 이 zip 의 **루트**(또는 폴더 하나로 감싼 그 안)에 오게 하십시오.
- `package-lock.json` 을 함께 넣으십시오. 없으면 재현 가능한 빌드가 안 됩니다.
- `.env`·`*.pem`·`*.key` 가 섞이지 않았는지 확인하십시오(`verify-zip.mjs` 가 봅니다).
- **`.env.example` 은 값을 비운 채로 넣으십시오.** 값이 빈 서식이라 통과시키는 것입니다.
  값은 `.env.local` 에 두십시오 — 그 파일은 애초에 zip 에 안 들어갑니다.

  ⚠ **검사기를 믿고 채워 넣지 마십시오.** 검사기가 잡는 것은 **알려진 형식**뿐입니다
  (`oqsk_`·AWS·개인키·결제 라이브 키·GitHub·Slack·Google·npm 토큰). `ZALKERA_REVALIDATE_SECRET`
  처럼 **임의의 랜덤 문자열**을 넣는 자리는 형식이 없어 **못 잡습니다**(실측). 즉 채워 넣은
  서식이 초록으로 통과할 수 있습니다.

  넣기 전에 **눈으로 한 번 여십시오.** 값이 남아 있으면 지우고 넣으십시오 —
  git 을 쓰신다면 `git checkout .env.example` 한 줄이면 원래 서식으로 돌아갑니다.

```bash
# 예 — 저장소 루트에서
zip -r ../내사이트.zip . -x 'node_modules/*' '.next/*' '.git/*' '.env*'

# ⚠ 서식은 **다시 넣습니다.** 위 `.env*` 는 `.env.example` 까지 zip 에서 빼기 때문입니다
#    (디스크의 파일이 지워지는 것은 아닙니다). README 의 첫 명령 `cp .env.example .env.local` 이
#    그 파일을 쓰므로, 빠지면 받으신 분이 첫 줄에서 막힙니다.
zip ../내사이트.zip .env.example

# 들어갔는지 **눈으로 확인하십시오** — 위 한 줄을 빠뜨려도 아래 검수는 초록입니다.
unzip -l ../내사이트.zip | grep '\.env\.example'

node scripts/verify-zip.mjs ../내사이트.zip
```

**사이트 데이터는 그대로입니다.** 페이지 내용·상품·주문·회원은 콘솔(데이터베이스)에 살고, 업로드는
**화면 코드만** 갈아 끼웁니다.

### 되돌리려면 — **올리기 전 zip 을 보관하십시오**

지금은 콘솔에 "이전 버전으로 되돌리기" 버튼이 없습니다. 대신 **직전에 올린 zip 을 다시 올리는 것이
곧 되돌리기**입니다. 소스는 당신 것이고 우리가 보관해 주지 않으므로, 올릴 때마다 그 zip 을 남겨
두십시오(`내사이트-2026-08-01.zip` 처럼 날짜를 붙이면 충분합니다).

되돌려도 **데이터는 영향받지 않습니다** — 화면 코드만 이전 것으로 돌아갑니다.

---

## 5. 어디서 고치는 게 가장 싸고 빠른가

세 갈래가 다 정상 경로입니다 — 콘솔·AI·손. 다른 점은 **드는 값과 걸리는 시간**입니다.

| 고치고 싶은 것 | 가장 싼 길 | 왜 |
|---|---|---|
| 색·글꼴·모서리·여백 | **소스**(§1 — `globals.css` 의 `@theme`) | 값이 한 자리에 있어 고치기 쉽습니다. 다시 빌드해야 반영됩니다 |
| 시작 페이지의 문구·이미지 | **소스**(§2 — `content/pages/home.json`) | 재업로드 1회. 콘솔에는 페이지 편집이 없습니다 |
| 소스에 박힌 문구·배너·섹션 구조 | **AI**(콘솔 "말로 고치기") 또는 손 | 화면 코드(JSX)를 고치는 일입니다. AI 는 §3 규약까지 함께 맞춰 주고, 손으로 하면 올리기 전에 `npm run validate` 로 직접 확인합니다 |
| 새 페이지·새 섹션 추가 | **AI** 또는 개발자 손 | 라우팅·계약·검사기 규약을 한꺼번에 맞추는 일입니다 |
| 이미 있는 디자인의 미세 조정(간격·크기) | 개발자 손 또는 AI | 둘 다 됩니다 |
| 프론트를 통째로 다시 짜기 | 개발자 손 | `docs/byo-headless-guide.md` 참고 |

**비용**: 콘솔에서 하는 것(데이터 편집·표현 설정)은 **무료**입니다. AI 가 **코드를 고치는 것**만
사용한 만큼 크레딧이 듭니다.

크레딧이 드는 쪽이 손해라는 뜻은 아닙니다 — 비교 대상은 "무료 편집"이 아니라 **개발사에 의뢰하고
견적을 받고 순서를 기다리던 종전**입니다. 콘솔에서 무료로 되는 것들은 원래 그 견적서에 있던 항목이
값 없이 즉시로 내려온 것이고, 코드를 고치는 것은 종전과 같은 일을 더 싸고 빠르게 하는 것입니다.
어느 쪽이 낫다는 서열은 없고, 값이 다른 단계가 있을 뿐입니다.

**AI 에게 맡길 때**는 `npm install` 후 생기는 `node_modules/@zalkera/client/llms.txt` 와 이 저장소의
`AGENTS.md` 를 함께 주십시오. 그 둘이 규약을 **말로** 담고 있고, 기계가 지키는 쪽은 저장소에 이미
들어 있습니다 — 고친 뒤 `npm run validate` 와 `npm run format` 을 한 번씩 돌리면 됩니다.
우리 콘솔의 AI 가 아니어도 됩니다 — **소스는 당신 것이고, 아무 AI 로나 고쳐서 다시 올리면 됩니다.**

---

## 6. 막혔을 때

| 증상 | 먼저 볼 것 |
|---|---|
| 업로드가 반려됨 | `node scripts/verify-zip.mjs 내zip` 의 ❌ 줄 |
| `@theme` 을 고쳤는데 그 자리만 안 바뀜 | `npm run validate` 의 **S2·S4** — 그 자리에 색 리터럴이나 인라인 style 이 있습니다 |
| 스타일 규약 검사가 갑자기 느슨해짐 | `package.json` 의 `zalkera.styling` 선언이 지워졌는지(§3) |
| 콘솔에서 넣은 섹션이 화면에 안 나옴 | `npm run validate` 의 **C2** — 렌더러가 그 섹션 종류를 모릅니다 |
| 사이트가 갑자기 느려짐 | `npm run validate` 의 **C1/C1b** |
| 개시했는데 진열이 비어 있음 | 정상입니다 — 상품·갈래는 콘솔이 주인이라 등록 전에는 비어 있습니다(§0) |
| 검색·AI 답변에 상품·회사가 안 잡힘 | `npm run check:aeo`(§3) 의 red 줄 — 구조화 데이터가 빠진 표면을 지목합니다 |
