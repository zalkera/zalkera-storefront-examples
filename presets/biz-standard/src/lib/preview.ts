/**
 * 미리보기 모드 판별. 미리보기 러너가 `next dev` 프로세스에 `NEXT_PUBLIC_ZALKERA_PREVIEW=1` 을
 * 주입한다. =1 이면 프로덕션 공개 API(읽기)만 소비하고 쓰기(체크아웃·장바구니 변경)를 차단해
 * 프로덕션 데이터를 오염시키지 않는다.
 *
 * ■ **구 이름을 지우지 마라 — 위험이 비대칭이다**
 *   `NEXT_PUBLIC_ONEQUE_PREVIEW` 를 넣는 **생산자는 지금 없다**(확장은 신 이름만 넣고, 백엔드 서빙
 *   스냅샷은 미리보기 플래그를 아예 안 싣는다). 그래서 「죽은 폴백」으로 보인다. 그런데 `README.md`
 *   가 **「지금도 받습니다」라고 명시**하고 있어, 그 말을 따라 손으로 그 이름을 넣어 둔 BYO 배포가
 *   있을 수 있다.
 *
 *   지웠을 때: 그 배포의 `isPreview()` 가 **거짓**이 되고 미들웨어가 쓰기를 안 막는다 — 미리보기가
 *   상용 백엔드에 주문·결제를 쓴다. 남겼을 때: 아무 일도 없다. 한쪽만 되돌릴 수 없다.
 *
 *   지우려면 README 의 그 문장을 먼저 걷고, 그 판이 고객에게 닿을 시간을 준 다음이다.
 */
// NEXT_PUBLIC_* 는 빌드 시 리터럴로 치환되므로 동적 키 접근이 불가하다 — 두 이름을 그대로 나열한다.
export const isPreview = (): boolean =>
    process.env.NEXT_PUBLIC_ZALKERA_PREVIEW === "1" || process.env.NEXT_PUBLIC_ONEQUE_PREVIEW === "1";

// ⚠ **여기에 적용 지점을 나열하지 마라.** 손으로 관리하던 목록이 둘에서 멈춰 있는 사이 카트 세 곳이
//   무방비가 되고, 그동안 `CUSTOMIZE.md` 는 "장바구니 쓰기가 막힌다"고 보증한다 — 미리보기
//   빌드에서 항목 삭제가 운영 백엔드까지 간다. 목록은 반드시 낡는다.
//
//   지금 집행하는 것은 **`src/middleware.ts` 하나**다 — 요청의 메서드·경로만 보므로 라우트가 어떤
//   형태로 쓰였든, 어디에 있든 덮인다. 아래 라우트들의 `if (isPreview())` 는 **이중 방어**로 남긴
//   것이고 집행 지점이 아니다. 면제는 `previewGuard.ts` 의 `PREVIEW_WRITE_ALLOW` 한 곳에 있다.
//
//   ⚠ 종전에는 이 규약을 소스를 **텍스트로 파싱**해 재는 시험이 있었고 **네 판 연속 뚫렸다.**
//   그 이력은 `previewGuard.ts` 머리말에 있다 — 파싱으로 되돌리지 마라.
