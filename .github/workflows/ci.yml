# 잘커라(Zalkera) 스토어프론트 Tier2 게이팅 CI.
#
# 백엔드 BuildGate 가 GitHub Checks API 로 이 워크플로의 성공을 확인한 뒤에만
# Tier2(코드 배포) 를 진행한다. 세 단계는 27-real-codegen-deploy-pipeline.md §7.1·§9.3(T0)
# 의 정본:
#   1) validate     — 키트 validator(scripts/validate-storefront.mjs, llms.txt §5 규약)
#   2) tsc --noEmit  — 타입 정합
#   3) next build    — 실제 빌드 성공
name: storefront-ci

on:
  push:
    # main 배포 + codegen 작업 브랜치(oneque/change-*) — 게이트가 브랜치 CI 결론을 읽으려면 브랜치 push 에도 돌아야 함.
    # oneque/client-upgrade-* 는 client-upgrade.yml 이 전담(lockfile 재생성 전 SHA 에서 npm ci 가 헛도는 red 방지).
    branches: [main, "oneque/change-*"]
  pull_request:

jobs:
  gate:
    runs-on: ubuntu-latest
    env:
      # 빌드 시점에 백엔드 접속이 필요 없도록 안전한 기본값만 주입(SSR fetch 는 런타임에 일어남).
      # ZALKERA_TENANT 는 실재하지 않아도 된다 — 러너에 백엔드가 없어 어차피 fetch 가 실패하고,
      # 코드가 폴백 없이 require 하므로 값 자체는 있어야 한다.
      # 이름은 서빙 인프라가 주입하는 것과 **반드시 같아야 한다**(백엔드 SandboxBuildGate.CI_ENV 패리티) —
      # 구 `ONEQUE_*` 로 두면 폴백이 제거된 지금 `env.ts` 가 던져 빌드가 죽는다(memo101 컷오버 실사고).
      ZALKERA_API_BASE: http://localhost:8100
      ZALKERA_TENANT: ci-placeholder
      # 이 러너에는 백엔드가 **없다**고 선언한다(src/lib/buildEnv.ts). 홈이 시드 페이지를 집어 오는 경로가
      # 불통을 그냥 던지면 여기서 빌드가 죽는다 — 반대로 테넌트 서빙 빌드는 던져야 맞으므로, 구분은
      # 상태값이 아니라 이 플래그로 한다(기본값은 fail-fast).
      ZALKERA_OFFLINE_BUILD: "1"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install
        run: npm ci

      - name: Validate (키트 validator)
        run: npm run validate

      - name: Typecheck (tsc --noEmit)
        run: npm run typecheck

      - name: Test (교차사이트 가드 회귀 픽스처)
        # memo118 §8 — 판정 규칙은 한 줄만 흔들려도 조용히 열린다. Node 기본 러너 + 타입 스트리핑이라
        # devDependency 추가가 0이다(node 22 필요). 검사기 X1 은 "가드를 불렀는가"만 보므로
        # **가드가 옳은가**는 이 스텝에서만 잠긴다.
        run: npm test

      - name: Build (next build)
        run: npm run build

      - name: 서빙 산출물 (.next/standalone/server.js)
        # memo145 — `next build` 성공은 **서빙된다**를 뜻하지 않는다. 잘커라 호스팅은 `next start` 가
        # 아니라 자기완결 산출물을 `node server.js` 로 띄우므로, standalone 이 안 나오면 이 레포의
        # 소스로 개시한 사이트는 서빙 박스에서 exit 4 로 반려된다(2026-08-01 실사고).
        # 재는 것은 next.config 의 문자열이 아니라 **방금 낸 산출물**이다 — 빌드는 이미 위에서 돌았고
        # (추가 비용 0), 설정 정적 판독은 조건부 조립 앞에서 양방향으로 거짓말한다(memo140 §6.5 X1).
        run: test -f .next/standalone/server.js

      - name: 방문자 IP 선언 (얼굴 안의 안전 배선)
        # 배선 동일성은 **바이트 잠금**이라 얼굴 파일을 못 본다. 그런데 그 얼굴 안에 안전 배선 한 줄이
        # 산다 — 2026-08-10 에 정확히 그 사각에서 skeleton 주문 상세 페이지의 선언이 빠진 채
        # CI 가 5일간 초록이었다. 이 검사는 바이트가 아니라 **선언의 존재**를 본다.
        run: node scripts/lib/visitor-ip-parity.mjs

      - name: 배선 동일성 (팩 4벌이 갈렸는지)
        # 팩은 각자 온전한 소스를 갖는다(오너 확정 2026-08-01). 얼굴이 갈리는 것은 의도이고,
        # **틀리면 사고가 나는 배선**만 바이트로 잠근다. npm 스크립트로 노출하지 않는 이유는
        # 이 파일이 고객 zip 에 안 실리기 때문이다 — 안 실리는 것을 가리키는 스크립트를 배송하면
        # 고객의 `npm run` 목록에 죽은 항목이 생긴다.
        # ⚠ **이 스크립트는 고객 zip 에 안 실린다**(SOURCE_EXCLUDES). 우리 레포에만 뜻이 있는 검사라
        # 그게 맞는데, ci.yml 은 **실린다** — 그래서 무조건 부르면 고객 레포에서 MODULE_NOT_FOUND 로
        # **CI 가 영구 적색**이 된다(재심의 차단 2 · 고객 트리에서 실측). 그 적색은 소음에 그치지 않는다:
        # 백엔드 BuildGate 가 이 워크플로의 성공을 읽고 Tier2 코드 배포를 진행하므로, 그 사이트는
        # "말로 고치기"가 구조적으로 막힌다. **3.0.6 라이브에도 이미 이 모순이 실려 있다.**
        #
        # 배송용 ci.yml 을 따로 두지 않는 이유: 두 벌이 되는 순간 갈린다(이 레포가 내내 싸우는 병이다).
        # 파일이 있을 때만 도는 편이 한 벌을 지키면서 양쪽에서 옳다.
        run: |
          if [ -f scripts/lib/wiring-parity.mjs ]; then
            node scripts/lib/wiring-parity.mjs
          else
            echo "배선 동일성은 잘커라 레포 전용 검사입니다(팩 4벌 대조) — 건너뜁니다."
          fi
