{
  "_": "가드 회귀 스위트의 파일별 통과 하한. `ci.yml` 이 이 파일을 읽는다. `verify-zip` 은 자기 `REQUIRED_FLOORS` 를 요구치로 쓰고 이 파일로는 **올리거나 늘릴 수만** 있다 — 낮추거나 비우면 반려한다(비워서 게이트를 끄는 길을 막는 자리다). 현재치는 `for f in src/lib/*.test.ts; do node --experimental-strip-types --test \"$f\" | grep '^# pass'; done` 로 잰다.",
  "src/lib/crossOrigin.test.ts": 14,
  "src/lib/oauthState.test.ts": 7,
  "src/lib/previewGuard.test.ts": 6,
  "src/lib/safeUrl.test.ts": 4
}
