# **`npm run format` 이 보는 것과 이 파일이 남기는 것이 같아야 한다.**
#
# 그 스크립트는 `src/**/*.{ts,tsx}` 와 `presets/**/*.{ts,tsx}` 만 본다. 그런데 에이전트가 이 저장소의
# prettier 설정을 "정본"으로 읽고 자연스러운 기본 동작인 `prettier --write .` 를 돌리면 설정·워크플로·
# 문서·콘텐츠 JSON·락파일까지 함께 재포맷된다 — AGENTS.md 포맷 절이 막으려는 바로 그 해악(고친 줄이
# 아니라 파일 전체가 바뀐 것으로 보이는 diff)이 그 순간 난다.
#
# 그래서 **의도를 문서가 아니라 파일로 적는다.** 여기 목록이 곧 "포맷터가 손대지 않는 것"이고,
# 남는 것은 정확히 `.ts`·`.tsx` 소스뿐이다.

# 데이터·콘텐츠 — 사람이 읽기 좋게 손으로 배열한 것들이다. 포맷터가 재배열하면 의미가 아니라
# 잡음만 만든다(콘텐츠 매니페스트·시드·픽스처·매니페스트류).
*.json
*.md

# 스타일 토큰 표는 토큰이 한눈에 보이게 손으로 정렬한다.
*.css

# 설정·워크플로·문서·도구 — 각자 다른 관례로 쓰였고 배송 계약의 일부다.
.github/
docs/
scripts/
qa/

# 생성물·배송 아티팩트
.next/
dist-presets/
dist-preview/
node_modules/
